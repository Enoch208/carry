# Internal working files — never commit
de.json
CLAUDE.md
PRD-*.md

# OS
.DS_Store

# SDD scratch / progress ledger + agent worktrees
.superpowers/
.claude/

# Node / build artifacts (monorepo-wide)
node_modules
.next/
out/
dist/
*.tsbuildinfo
.env*.local
.vercel
