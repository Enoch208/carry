{
  "name": "@carry/walrus",
  "version": "0.1.0",
  "private": true,
  "description": "Walrus storage adapters for Carry — store and verify memory blobs over the Walrus HTTP API.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  }
}
